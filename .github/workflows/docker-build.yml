name: container
on: 
  push:
   branches: [ master ]

jobs:
  node-docker:
    runs-on: ubuntu-latest
    container:
      image: node:14.16
      env:
        NODE_ENV: development
      ports:
        - 80
      volumes:
        - my_docker_volume:/volume_mount
    steps:
         - name: build insecure bank
           run: |
              java --version
              #bash <(curl -s https://detect.synopsys.com/detect8.sh) --blackduck.url=https://testing.blackduck.synopsys.com --blackduck.trust.cert=true --detect.tools=DETECTOR,SIGNATURE_SCAN,IAC_SCAN
